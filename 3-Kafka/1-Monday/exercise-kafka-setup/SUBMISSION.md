# Submission

The exercise has been completed and verified.

The following evidence files are included in the exercise folder:

- `kafka-ui-screenshot` – Screenshot of the Kafka UI showing the running cluster.
- `topic-list-screenshot` – Screenshot showing the Kafka topic list.
- `python-verification-output.txt` – Output from running the Kafka connection verification script.

These files confirm that the Kafka cluster was running and that the Python verification script successfully connected to the broker.

# Issues Encountered

No system configuration issues were encountered.

The only issues  were related to not carefully reading the instructions.  
A few steps were initially misinterpreted, but after reviewing the instructions and troubleshooting the output, the mistakes were identified and corrected.

