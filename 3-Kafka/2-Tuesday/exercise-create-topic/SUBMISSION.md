# Submission

The exercise has been completed and verified.

The following evidence files are included in the exercise folder:

- `describe-topics-screenshot,png` – Screenshot of CLI topics create.
- `create-topics-output.txt` – Output from running the create topic script.
- `python-verification-output.txt` – Output from running the inspect topic script.

These files confirm that CLI describe command was running and that both Python scripts was successful.